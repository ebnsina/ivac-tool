# Optional — enables the Tier-2 AI fallback when deterministic extraction fails.
# Without it the app still works for known bundle shapes; the UI just shows
# "AI available: false".
ANTHROPIC_API_KEY=

# Optional — override the model used by the AI fallback (default claude-sonnet-4-6)
# IVAC_AI_MODEL=claude-sonnet-4-6
